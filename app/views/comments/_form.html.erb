<% if user_signed_in? %>
<%= form_for([@post, @post.comments.build]) do |f| %>
<p>
<%= f.label :name %><br>
<%= f.text_field :name %>
</p>
<p>

<%= f.label :body %><br>
<%= f.text_area :body %>
</p>
<br>
<p>
<button type %= f.submit % class="btn btn-primary">Create a Comment</button> 
</p>
  <% end %>
 <% end %>